<div style=" width:10000px; ">

  <div id="<%= "m5#{profile.idNo}" %>" style="float:left; width:920px;display:block; height: 560px; overflow-y: auto">

    <div style=" width:800px;margin: 0px auto;">
      <h1>Payment Schedule  <span class="sml"> <% tooltip :id => "attendance_tip" do %>
  <%=" <p>The fees for the whole semester excluding those that are not presented in the original schedule of fees at the start of the semester such as, yearbook, graduation and breakage fees.</p>
<br/>" %>
<% end %></span></h1>
        
     <b>Tuition Fee Breakdown</b>
<table border="1" cellspacing="0" cellpadding="0" width="741" class="c">

      </table>
<% if@tf_breakdown.size>0 %>

      <table border="1" cellspacing="0" cellpadding="0" width="541" class="c">
        <tr class="d">
          <td width="100" valign="top"><p>Item</p></td>
          <td width="200" valign="top"><p>Fee Amount</p></td>
        </tr>
        <% @tf_breakdown.each do |item| %>
          <tr><td ><%="  "+item.item %></td><td  align="right"><%= " Php #{number_with_precision(item.feeAmt ,2, :separator =>".")}"%></td></tr>
        <% end %>

      </table>
  <% else %>
 
  <% end %>

      <br/>
       <b>Assessment</b>
<% if @tf_assessment.size >0%>
 <table border="1" cellspacing="0" cellpadding="0" width="741" class="c">
        <tr class="d">
          <td width="100" valign="top"><p>Grading Term</p></td>
           <td width="300" valign="top"><p>Balance As Of</p></td>
          <td width="200" valign="top"><p>Payment Amount</p></td>

        </tr>
        <% @tf_assessment.each do |item| %>
          <tr><td ><%= case item.gradingTerm
          when "P"
            "Prelims"
          when "M"
            "Midterms"
          when "F"
            "Finals"
          end
          %></td><td align="right"><%= " Php #{number_with_precision(item.balanceAsOf ,2, :separator =>".")}"%></td><td align="right"><%=  " Php #{number_with_precision(item.payAmt ,2, :separator =>".")}"%></td></tr>
        <% end %>

      </table>
  <% else %>
<table border="1" cellspacing="0" cellpadding="0" width="741" class="c">
            <tr class="d">No assessment information available.</tr></table>
  <% end %>
   <br/>
   <b>Payments</b>
   <% if @payments.size > 0 %>
<table border="1" cellspacing="0" cellpadding="0" width="741" class="c">
        <tr class="d">
          <td width="150" valign="top"><p>Date of payment</p></td>
           <td width="300" valign="top"><p>Details</p></td>
          <td width="200" valign="top"><p>Amount Paid</p></td>
         
        </tr>
        <% @payments.each do |payment| %>
          <tr><td ><%= payment.dateOfPayment.strftime("%B %d, %Y") %></td><td  ><%= payment.textDetail%></td><td align="right"><%=  " Php #{number_with_precision(payment.amt,2, :separator =>".")}"%></td></tr>
        <% end %>

      </table>
  <% else %>
<table border="1" cellspacing="0" cellpadding="0" width="741" class="c">
            <tr class="d">No payments information available.</tr></table>
  <% end %>
      

<br/>





     

    </div>

  </div>
</div>


